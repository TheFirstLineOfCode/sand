CREATE TABLE FOLLOWS(id VARCHAR(64) PRIMARY KEY, friend VARCHAR(64) NOT NULL, event VARCHAR(64) NOT NULL, follower VARCHAR(64) NOT NULL, approver VARCHAR(32) NOT NULL, approval_time TIMESTAMP NOT NULL, CONSTRAINT UNIQUE_FRIEND_EVENT_FOLLOWER UNIQUE (friend, event, follower));
CREATE INDEX INDEX_FOLLOWS_FRIEND_EVENT ON FOLLOWS (friend, event);
